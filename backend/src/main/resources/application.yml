server:
  port: ${PORT:8080}

spring:
  application:
    name: ai-context-generator
  mustache:
    suffix: .mustache
    check-template-location: true

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

logging:
  level:
    com.aicontext: ${LOG_LEVEL:DEBUG}
