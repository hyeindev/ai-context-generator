{
  "id": "domain",
  "title": "도메인 설계",
  "description": "엔티티, 값객체, 연관관계 규칙을 설정합니다.",
  "options": [
    {
      "id": "idStrategy",
      "label": "ID 생성 전략",
      "type": "select",
      "required": true,
      "values": [
        { "id": "auto-increment", "label": "Auto Increment", "description": "DB 자동 증가 (Long)" },
        { "id": "uuid", "label": "UUID", "description": "범용 고유 식별자" },
        { "id": "ulid", "label": "ULID", "description": "시간순 정렬 가능한 UUID" },
        { "id": "snowflake", "label": "Snowflake", "description": "Twitter 스타일 분산 ID" }
      ]
    },
    {
      "id": "valueObjectUsage",
      "label": "값객체(VO) 사용 기준",
      "type": "multiSelect",
      "required": false,
      "values": [
        { "id": "email", "label": "Email", "description": "이메일 주소" },
        { "id": "money", "label": "Money", "description": "금액 (통화 포함)" },
        { "id": "phone", "label": "PhoneNumber", "description": "전화번호" },
        { "id": "address", "label": "Address", "description": "주소" },
        { "id": "date-range", "label": "DateRange", "description": "기간 (시작~종료)" }
      ]
    },
    {
      "id": "associationFetchDefault",
      "label": "연관관계 기본 로딩",
      "type": "select",
      "required": false,
      "values": [
        { "id": "lazy", "label": "LAZY", "description": "지연 로딩 (권장)" },
        { "id": "eager", "label": "EAGER", "description": "즉시 로딩" }
      ]
    },
    {
      "id": "softDelete",
      "label": "Soft Delete 사용",
      "type": "boolean",
      "required": false,
      "description": "삭제 시 deleted_at 컬럼 사용"
    },
    {
      "id": "auditFields",
      "label": "Audit 필드",
      "type": "multiSelect",
      "required": false,
      "values": [
        { "id": "created-at", "label": "createdAt", "description": "생성 일시" },
        { "id": "updated-at", "label": "updatedAt", "description": "수정 일시" },
        { "id": "created-by", "label": "createdBy", "description": "생성자" },
        { "id": "updated-by", "label": "updatedBy", "description": "수정자" }
      ]
    },
    {
      "id": "entityEquality",
      "label": "엔티티 동등성 기준",
      "type": "select",
      "required": false,
      "values": [
        { "id": "id-only", "label": "ID만 비교", "description": "id로만 equals/hashCode" },
        { "id": "business-key", "label": "비즈니스 키", "description": "의미있는 필드로 비교" }
      ]
    }
  ]
}
